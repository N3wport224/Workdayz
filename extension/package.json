{
  "name": "workdayz-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Workdayz browser extension: autofills Workday job applications from a tailored resume/cover letter package.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:firefox": "node build.mjs && node scripts/build-firefox.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node test/run-e2e.mjs",
    "test:popup": "node test/popup-verify.mjs",
    "package": "npm run build && cd dist && zip -qr ../workdayz-extension.zip . -x '*.map' && cd .. && echo 'wrote workdayz-extension.zip'",
    "generate-icons": "node scripts/generate-icons.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "esbuild": "^0.25.0",
    "playwright-core": "^1.61.1",
    "pngjs": "^7.0.0",
    "typescript": "^5.7.0"
  }
}
